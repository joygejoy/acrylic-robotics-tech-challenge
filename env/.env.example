# Backend API Configuration
# Copy this file to .env.local and update with your actual URLs
# Vite loads from env/ - prefix variables with VITE_
# Latest = current production; Pinned = rollback version (can be same URL until you have two deployments).
VITE_API_BASE_URL_LATEST=https://your-project.vercel.app
VITE_API_BASE_URL_PINNED=https://your-project.vercel.app
